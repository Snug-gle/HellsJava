<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itwill.helljava.mapper.PayMapper">
	<insert id="insertPay">
		<selectKey resultType="int" keyProperty="num" order="BEFORE">
			select pay_seq.nextval from dual
		</selectKey>
		insert into values(#{num}, #{member_no}, #{account_bank}, #{account_number}, #{account_pw}) 
	</insert>
	
	<delete id="deletePay">
		delete from pay where member_no = #{member_no}
	</delete>
	
	<select id="selectPayCount" resultType="int">
		select count(*) from pay where member_no = #{member_no}
	</select>
	
	<select id="selectPay" resultType="Pay">
		select * from pay where pay_no = #{pay_no} 
	</select>
	
	<select id="selectPayList" resultType="Pay">
		select * from (select rownum rn,board.* from 
		(select * from pay where member_no = #{member_no} order by pay_no desc)
		board) where rn between #{startRow} and #{endRow}
	</select>
</mapper>